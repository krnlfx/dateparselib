# Usage

## Install

```bash
pip install -r requirements.txt
```

## Run

```bash
python -m dateparselib --help
```

## Configure

Create a config file (JSON or YAML) and point the tool at it:

```bash
python -m dateparselib run --config config.json
```